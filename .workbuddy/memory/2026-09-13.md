# 2026-09-13

## UI 改造：Theme 体系 + Vault 导入弹窗（commit cc20e01）
- 拆分 `assets/styles/` 为 tokens.css(骨架) / theme-dark.css / theme-light.css；base.css 增全局 :focus-visible 与 .sr-only。
- 新增 `stores/theme.ts`（system/light/dark，localStorage `obsidianmind-theme`，跟随系统）、`components/theme/{ThemeSwitcher,ThemeQuickToggle}`、`components/ui/AppModal.vue`。
- 重写 Vault 导入弹窗：`VaultConnectModal`（编排+错误转译）/`VaultPicker`（拖拽落区+Primary+Loading）/`LocalFirstNotice`；`VaultWelcome` 变兼容入口。新增拖入目录导入（getAsFileSystemHandle）。
- vault store：新增 `connecting`、`connectHandle`，改为扫描完成后才关弹窗（失败保持打开显示错误）。
- 全站硬编码 hex/rgba 清零（含 KnowledgeGraph 玻璃层、NoteView 警示条、SettingsView 徽标/开关等），新增 --primary-solid/--on-primary/--overlay/--glass-*/--focus-ring 等语义 token。
- 修复：① `.gitignore` `Vault/`→`/Vault/`（macOS core.ignoreCase 曾把 src/components/vault/ 整目录忽略，新组件不入库）；② 补齐被引用但未定义的 `--sp-7`/`--r-xl`（原弹窗 padding/radius 实际失效）。
- 文档：README 新增 "Frontend UI / Design System"；新增 docs/development/design-system.md；development/README 增 Token 约定。
- 验证：npm run build 零错误；Playwright 实测三态主题/刷新持久化/5 档分辨率无横向溢出/ESC+遮罩关闭/演示 Vault 正常。
- 未做（明确说明）：未引入 Toast 体系（错误用 Modal 内联态）；未做 E2E 自动化测试文件；拖拽在 Safari/Firefox 不支持（降级为点击选择）。
