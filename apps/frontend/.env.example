# Frontend 环境变量示例
# 复制为 .env.local（本地覆盖，不入 Git）或 .env.production

# 后端 Spring Boot API 地址
# 留空/注释 = 使用 Vite dev server 的 /api 代理（开发模式推荐，见 vite.config.ts）
# 显式指定 = 直连后端（生产部署时使用）
# VITE_API_BASE_URL=http://localhost:8080
